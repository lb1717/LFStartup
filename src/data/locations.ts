export interface Location {
  id: string;
  name: string;
  universityId: string;
  building: string;
  floor?: string;
  room?: string;
  description?: string;
  exactAddress?: string;
  createdAt?: string;
} 